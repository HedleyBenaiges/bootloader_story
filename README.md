# Bootloader Story

An unskippable very long joke randomly selected from "/stories" and printed to the terminal one line at a time. The program also catches `^C` to avoid being skipped.

## Installation
Run the install.sh
Hopefully it works, I haven't tested it
